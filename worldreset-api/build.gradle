plugins {
    id 'fabric-loom'
}

base.archivesName = "worldreset-api"

loom {
    mods {
        "worldreset-api" {
            sourceSet sourceSets.main
        }
    }
}

dependencies {
    modImplementation("net.casualchampionships:arcade-dimensions:${project.arcade_version}")
}
